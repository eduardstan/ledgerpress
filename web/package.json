{
  "name": "ledgerpress-web",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "astro": "astro",
    "build": "astro build && pagefind --site dist",
    "check": "astro check",
    "dev": "astro dev",
    "prebuild": "npm run stage-media",
    "predev": "npm run stage-media",
    "prepreview": "npm run stage-media",
    "prestart": "npm run stage-media",
    "preview": "astro preview",
    "stage-media": "rm -rf public/media && mkdir -p public/media && if [ -d ../content/media ]; then cp -R ../content/media/. public/media/; fi",
    "start": "astro dev",
    "test": "node --test ../scripts/agents-md.test.mjs && node --experimental-strip-types src/lib/urls.test.ts && node --experimental-strip-types src/lib/record.test.ts && node --experimental-strip-types src/lib/cv.test.ts && node --experimental-strip-types src/lib/consistency.test.ts && node --experimental-strip-types src/lib/announcements.test.ts"
  },
  "dependencies": {
    "@astrojs/markdown-remark": "7.2.1",
    "@astrojs/mdx": "7.0.4",
    "@astrojs/rss": "4.0.19",
    "@astrojs/sitemap": "3.7.3",
    "astro": "7.1.4",
    "katex": "0.16.47",
    "rehype-katex": "7.0.1",
    "remark-math": "6.0.0",
    "yaml": "2.9.0"
  },
  "devDependencies": {
    "@astrojs/check": "0.9.10",
    "@tailwindcss/vite": "4.3.3",
    "pagefind": "1.5.2",
    "tailwindcss": "4.3.3",
    "typescript": "6.0.3"
  },
  "allowScripts": {
    "esbuild": true
  }
}
