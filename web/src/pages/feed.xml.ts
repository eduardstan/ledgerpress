/**
 * `/feed.xml` — the address `jekyll-feed` published the Atom feed at, kept
 * alive because feed readers are subscribed to it and a meta-refresh redirect
 * would not reach them. It serves the same document as `/rss.xml`.
 */
export { GET } from './rss.xml';
